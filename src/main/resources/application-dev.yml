spring:
  datasource:
    url: jdbc:mysql://localhost:3306/university_booking
    username: root
    password: "your Password"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

